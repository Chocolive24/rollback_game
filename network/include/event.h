#pragma once

#include <Common-cpp/inc/defines.h>

enum class EventCode : nByte {
  kLeft = 0,
  kRight,
  kJump,
  kShoot
};

enum class EventKey : nByte {
  kLeft = 0,
  kRight,
  kJump,
  kShoot
};